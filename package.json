{
  "name": "position",
  "version": "0.0.1",
  "description": "Position elements relative to other elements",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && tsc && npm run demo:build && cp package.json dist/",
    "lint": "tslint",
	"demo:server": "parcel demo/index.html -d dist/demo",
	"demo:build": "parcel build demo/index.html -d dist/demo"
  },
  "author": "cal-smith",
  "license": "MIT",
  "devDependencies": {
    "husky": "^1.3.1",
    "parcel-bundler": "^1.12.1",
    "semantic-release": "^15.13.3",
    "tslint": "^5.14.0",
    "typescript": "^3.3.3333"
  }
}
